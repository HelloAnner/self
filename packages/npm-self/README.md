# @helloanner/self

This meta-package is an installation launcher only. It selects an exact-version platform package and forwards arguments, standard streams, signals, and the exit status to the standalone Self binary. It contains no Self business logic and performs no install-time download.
